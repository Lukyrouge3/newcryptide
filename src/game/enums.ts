export enum StructureType {
    STONE,
    CABIN,
    NONE
}

export enum StructureColor {
    BLACK,
    WHITE,
    GREEN,
    BLUE
}

export enum AnimalType {
    PUMA,
    BEAR,
    NONE
}

export enum Biome {
    FOREST,
    DESERT,
    MOUNTAIN,
    WATER,
    SWAMP
}